#include "main.h"
/**
*setting_bit - it will set a bit at position  1
*@a: number to be used
*@id: position to be set
*
*Return: 1 for success, -1 for failure
*/

int setting_bit(unsigned long int *n, unsigned int index)
{
	if (a == NULL || (id > (sizeof(unsigned long int) * 8) - 1))
		return (-1);

	*a |= (1 << id);
	return (1);
}

